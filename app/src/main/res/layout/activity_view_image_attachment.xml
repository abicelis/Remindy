<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_view_image_attachment_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    >

    <android.support.v7.widget.Toolbar
        android:id="@+id/activity_view_image_attachment_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        app:theme="@style/ToolbarTheme"
        />

    <com.github.chrisbanes.photoview.PhotoView
        android:id="@+id/activity_view_image_attachment_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/activity_view_image_attachment_toolbar"
        android:transitionName="@string/transition_image_attachment_image"

    />
    <!--android:scaleType="fitCenter"
            android:src="@drawable/icon_remindy"
-->


    <android.support.design.widget.FloatingActionButton
        android:id="@+id/activity_view_image_attachment_edit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentBottom="true"
        android:layout_marginRight="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        app:elevation="0dp"
        app:backgroundTint="@color/primary"
        android:src="@drawable/icon_edit_fab_mini"/>
</RelativeLayout>




